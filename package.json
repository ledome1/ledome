{
  "name": "ledome-mgmt",
  "version": "0.1.0",
  "private": true,
  "description": "LE DOME construction management platform",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
