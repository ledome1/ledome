{
  "name": "constructflow",
  "version": "0.1.0",
  "private": true,
  "description": "Clean-room construction operations platform starter",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
